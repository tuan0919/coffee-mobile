<resources>
    <!-- Base application theme. -->
    <style name="Theme.CoffeShop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ThemeOverlay.SearchView" parent="">
        <!--change hint color-->
        <item name="android:textColorHint">@android:color/white</item>
        <!--change text color-->
        <item name="android:editTextColor">@android:color/white</item>
    </style>

    <style name="CheckoutActiveButton">
        <item name="android:background">@drawable/checkout_activate_button</item>
        <item name="android:textColor">@color/Greyscale3</item>
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="android:textSize">22sp</item>
        <item name="android:radius">10dp</item>
    </style>

    <style name="CheckoutDeactiveButton">
        <item name="android:background">@color/Greyscale3</item>
        <item name="android:strokeColor">@color/Greyscale3</item>
        <item name="android:strokeWidth">2</item>
        <item name="android:textColor">@color/Background</item>
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="android:textSize">22sp</item>
        <item name="android:radius">10dp</item>
    </style>

    <style name="EditButton">
        <item name="android:background">@drawable/black_button_yellow_stroke</item>
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="android:textColor">@color/Primary</item>
        <item name="android:textSize">16sp</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.AppCompat.Light.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar" parent="Base.Theme.AppCompat.Light.NoActionBar" />
</resources>